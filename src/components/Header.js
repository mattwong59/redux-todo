import React from 'react'
import Footer from './Footer'

const Header = () => (
    <header>
        <h2 className='title is-3 has-text-left title__main'>REDUX ToDo</h2>
        <Footer />
    </header>

)

export default Header;